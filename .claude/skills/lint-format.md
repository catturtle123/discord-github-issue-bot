# Lint and Format

Run linting and formatting checks on the codebase.

## Steps
1. Run `ruff check .` to check for lint errors
2. Run `ruff check . --fix` to auto-fix fixable issues
3. Run `ruff format .` to format all Python files
4. Run `ruff format . --check` to verify formatting without changes
